const initialState = {
  searchTerms: '',
  results: [],
  isDropdownOpen: false,
  sortedBy: ''
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}