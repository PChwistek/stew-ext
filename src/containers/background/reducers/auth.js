const initialState = {
  'loggedIn': false,
  'loggedInAs': '',
  'jwt': '',
  'updatedOn': ''
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}